suitcase = ["shirt", "shirt", "pants", "pants", "pajamas", "books"]

#Changing beginning so that it only selects the first two elements of beginning
beginning = suitcase[0:2]

#Create a new variable that only chooses the two "pants" strings from the list
middle = suitcase[2:4]

#Print commands
print(beginning)
print(middle)